import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xff254EDB);
  static const scaffoldBackground = Colors.white;
  static const appBarColor = Colors.white;
  static const bottomNavLightThemeBackground = Colors.white;
  static const buttonBackGroundColor = Color(0xff254EDB);
  static const borderColor = Colors.black;

  static const textColor = Colors.black;
    static const activeTextColor = Color(0xff254EDB);
  static Color successColor = Colors.green.shade900;
  static const errorColor = Color(0xFFEF2828);
}
